import React from 'react';
import styles from './Footer.css';

const Footer = () => {
  return (
    <footer className={styles.Footer}>
      <span>
        By a React Master(hire me)
      </span>
    </footer>
  );
};

export default Footer;
